var eps = 0.00001;
var itr = /* ここを埋める */;

var min = -2;
var max = 2;

// 区間を itr 等分した時の小区間の幅
var h = /* ここを埋める */;

//　区間を小区間に分割して、各小区間に対して二分法を行う
for(var i = 0; i < itr ; i++){
	
	//小区間の端点を a,b とする
	var a = /* ここを埋める */; 
	var b = /* ここを埋める */;
	
	//　小区間の端点の関数値が異なっていたら二分法を行う
	/* ここを埋める */
	
	}
}

function f(x){
	return 2*x*x*x*x*x - 5*x  - 3;
}


